version: "3"

services:
  mc:
    image: ghcr.io/astralbijection/mc-ozone3:main
    container_name: ozone3
    tty: true
    volumes:
      - "/persist/mc/world:/srv/world"
      - "/persist/mc/backups:/srv/backups"
      - "./ops.json:/srv/ops.json"
      - "./whitelist.json:/srv/whitelist.json"
      - "./banned-ips.json:/srv/banned-ips.json"
      - "./banned-players.json:/srv/banned-players.json"
      - "./server.properties:/srv/server.properties"
    ports:
      - 25565:25565
    restart: unless-stopped
