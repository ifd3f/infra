repositories:
  - name: longhorn
    url: https://charts.longhorn.io
releases:
  - name: longhorn
    namespace: longhorn-system
    chart: longhorn/longhorn
    installed: true
    values:
      - ingress:
          enabled: true
          host: longhorn.s.astrid.tech
