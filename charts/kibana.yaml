ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
  hosts:
    - kibana.astrid.tech
  paths:
    - /

resources:
  requests:
    cpu: "100m"
    memory: "256Gi"
  limits:
    cpu: "1000m"
    memory: "256Gi"
